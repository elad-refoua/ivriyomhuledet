# מסירת פרויקט מלאה — עבריולדת

עודכן: 3 בספטמבר 2026

## 1. מטרת המוצר

עבריולדת הוא אתר עברי פשוט וברור שמאפשר לכל מבקר:

1. להזין שם ותאריך לידה לועזי או עברי.
2. לקבל תאריך עברי תקין, לרבות טיפול בשנת לידה באותיות.
3. לראות באתר, לכל אדם, את 20 ימי ההולדת העבריים הקרובים והתאריך הלועזי המקביל בכל שנה.
4. להוריד קובץ `.ics` מסודר שאפשר לייבא ליומנים נפוצים.
5. בעת הפעלת OAuth — לחבר את חשבון Google האישי ולסנכרן ישירות ליומן נפרד.

הכתובת הפעילה בעת המסירה: <https://ivriyomhuledet.elad.chatgpt.site>

## 2. מצב נוכחי

האתר מעוצב, רספונסיבי, RTL ופועל כאתר סטטי. קוד החיבור הישיר ל־Google Calendar כבר קיים, אך הכפתור מושבת עד הכנסת Google OAuth Client ID תקין.

גרסת המקור שפורסמה נלקחה מה־commit:

```text
107d8bfb89741c3ed579ce391ddc9267f5fdb895
```

פרויקט Sites הקיים:

```text
appgprj_6a99a74da9208191b3b784944453aa7a
```

הערך נשמר גם ב־`.openai/hosting.json`. אם ממשיכים את האתר הקיים, יש להשתמש בו כפי שהוא ולא ליצור פרויקט חדש.

## 3. ארכיטקטורה וקבצים

אין framework ואין build step. זו אפליקציית HTML/CSS/JavaScript מודולרית:

| קובץ | אחריות |
|---|---|
| `dist/index.html` | מבנה המסכים, טופס, רשימה, אזור Google, טעינת Google Identity Services |
| `dist/styles.css` | העיצוב המלא, RTL, responsive, מצבי פוקוס ושגיאה |
| `dist/app.js` | מצב האפליקציה, טופס, עריכה/מחיקה, תצוגת רשימה, הורדת ICS |
| `dist/calendar.js` | לוגיקת התאריך העברי, תאריכים עתידיים, ICS ומבנה אירועי Google |
| `dist/google-calendar.js` | OAuth token model וקריאות Google Calendar API |
| `dist/privacy.html` | מדיניות פרטיות |
| `dist/terms.html` | תנאי שימוש |
| `dist/vendor/hdate/` | ספריית `@hebcal/hdate` מקומית, כולל sourcemaps ורישיון |
| `.openai/hosting.json` | הגדרת הפרסום של Sites וה־project ID הקיים |

הספרייה הוכנסה מקומית כדי שהמרת התאריכים לא תהיה תלויה ב־CDN חיצוני בזמן טעינת האתר.

## 4. מה כבר עובד

### הזנת תאריך

- מצב לועזי: שדה תאריך, עם אפשרות "הלידה הייתה אחרי השקיעה". בחירה זו מוסיפה יום לפני ההמרה לעברי.
- מצב עברי: יום, חודש ושנה.
- בדיקה שתאריך הלידה אינו בעתיד.
- בדיקה שאדר ב׳ מופיע רק בשנה מעוברת.
- בדיקה שהתאריך העברי קיים בפועל.
- תצוגה מקדימה לפני השמירה.

### קלט ותצוגה של שנה עברית

הפונקציה `parseHebrewYear` מקבלת את כל הצורות הבאות וממירה אותן ל־5749:

- `תשמ״ט`
- `התשמ״ט`
- `תשמט`
- `התשמט`
- `5749`

הפלט עובר נרמול לתצוגה תקנית עם גרשיים. דוגמה מלאה:

```text
כ״א באלול תשמ״ט
```

היום והשנה מוצגים באותיות. התחילית `ה` של האלפים מתקבלת בקלט, אך אינה נחוצה בתצוגה המקוצרת המקובלת.

### רשימת אנשים

- הוספה, עריכה ומחיקה של אדם.
- מחיקת הרשימה כולה עם אישור.
- מיון לפי יום ההולדת הקרוב.
- לכל כרטיס יש פרטי תאריך הלידה, גיל קרוב והעדפת תזכורת.
- כל כרטיס נפתח לרשימה של 20 מועדים עתידיים: תאריך עברי, תאריך לועזי וגיל.
- הנתונים נשמרים מקומית בדפדפן תחת המפתח `ivriyomhuledet.people.v1`.

מבנה אדם נשמר כך:

```json
{
  "id": "uuid-or-local-id",
  "name": "שם האדם",
  "birth": { "yy": 5749, "mm": 6, "dd": 21 },
  "reminder": "evening"
}
```

ערכי התזכורת הנתמכים: `evening`, `morning-before`, `three-days`, `none`.

### חישוב ימי הולדת

האתר משתמש ב־`birthdayOrAnniversary` של `@hebcal/hdate`. נוצרים 20 מופעים מפורשים, מפני שאירוע לועזי חוזר אינו יכול לשמור על אותו יום עברי בכל שנה. הלוגיקה של אדר ושנים מעוברות נשענת על הספרייה ואין לשכתב אותה ידנית בלי מערך בדיקות מלא.

### קובץ ICS

`buildCalendarFile` יוצר לוח בשם "ימי הולדת עבריים" ו־20 אירועים לכל אדם. כל אירוע הוא אירוע של יום שלם ומכיל:

- כותרת: `🎂 יום ההולדת העברי של [שם] — [יום וחודש עבריים]`.
- התאריך העברי המלא באותה שנה.
- התאריך הלועזי באותה שנה.
- תאריך הלידה העברי המקורי.
- הסבר שהיום העברי מתחיל בשקיעה בערב שלפני.
- תזכורת לפי בחירת המשתמש.
- UID יציב המבוסס על מזהה האדם והשנה העברית.

שם הקובץ המורד: `ימי-הולדת-עבריים.ics`.

### Google Calendar

המימוש ב־`dist/google-calendar.js`:

- משתמש ב־Google Identity Services OAuth 2.0 token model.
- מבקש רק את ה־scope `https://www.googleapis.com/auth/calendar.app.created`.
- יוצר יומן נפרד בשם "ימי הולדת עבריים" באזור הזמן `Asia/Jerusalem`.
- שומר רק את מזהה היומן המקומי תחת `ivriyomhuledet.googleCalendarId.v1`.
- שומר access token בזיכרון בלבד; הוא לא נשמר ב־localStorage.
- יוצר מזהי אירועים יציבים באמצעות SHA-256 כדי שסנכרון חוזר יעדכן במקום להכפיל.
- מוחק מן היומן הייעודי אירועים ישנים שנוצרו על ידי האפליקציה ואינם ברשימה החדשה.
- מוסיף מאפיין פרטי `app=ivriyomhuledet` כדי לזהות רק אירועים מנוהלים.
- מבצע בקשות בקבוצות קטנות ומציג התקדמות ושגיאות בעברית.

## 5. הפעלת Google Calendar

זהו החלק היחיד שחסר להפעלה מלאה בפרודקשן.

1. פתח פרויקט ב־Google Cloud Console.
2. הפעל את **Google Calendar API**.
3. הגדר את Google Auth Platform: Branding, Audience ו־Data Access.
4. אם האפליקציה במצב Testing, הוסף את כתובות המשתמשים המורשים כ־test users.
5. צור OAuth Client מסוג **Web application**.
6. הוסף Authorized JavaScript origin מדויק:

   ```text
   https://ivriyomhuledet.elad.chatgpt.site
   ```

7. לפיתוח מקומי אפשר להוסיף גם origin מתאים, למשל `http://localhost:8000`.
8. העתק את ה־Client ID בלבד והכנס אותו ב־`dist/index.html`:

   ```html
   <body data-google-client-id="YOUR_CLIENT_ID.apps.googleusercontent.com">
   ```

9. **אין להכניס Client Secret**. זוהי אפליקציית דפדפן, ולכן סוד בקוד הקדמי אינו סוד.
10. בדוק בחשבון test שהתחברות, יצירת יומן, סנכרון חוזר, עריכה ומחיקה פועלים.
11. פרסם מחדש. אם הדומיין משתנה, עדכן גם את Authorized JavaScript origins.

ייתכן ש־Google תדרוש השלמת מסך הסכמה, אימות דומיין או אימות מותג/אפליקציה בהתאם למצב הפרסום, סוג המשתמשים והמדיניות העדכנית. דפי הפרטיות והתנאים כבר קיימים, אך יש לעדכן בהם פרטי מפעיל ויצירת קשר אמיתיים לפני הפצה רחבה.

## 6. פרטיות ואבטחה

מצב ברירת המחדל הוא local-first:

- שמות ותאריכי לידה נשמרים במכשיר בלבד.
- אין backend ואין database.
- אין analytics ואין tracking בקוד הנוכחי.
- רק בעת לחיצה יזומה על Google הנתונים הנחוצים נשלחים ל־Google Calendar API.
- ניתוק Google מבטל את ה־token שבזיכרון, אך אינו מוחק אוטומטית את היומן שכבר נוצר.
- מחיקה באתר משנה את הרשימה המקומית; האירועים נמחקים מהיומן בסנכרון הבא.

אם בעתיד מוסיפים חשבונות משתמשים, שיתוף רשימות, שרת או analytics, יש לעדכן את ארכיטקטורת האבטחה, בקשת ההסכמה ומדיניות הפרטיות לפני הפרסום.

## 7. Facebook — לא ממומש

אין בחבילה תוסף Facebook ואין איסוף ימי הולדת של חברים. אין לבצע scraping או הרחבת דפדפן שמעתיקה מידע אישי בלי בדיקה מחודשת של הרשאות Meta, תנאי הפלטפורמה והסכמה מפורשת. כחלופה בטוחה יותר אפשר בעתיד להוסיף יבוא ידני או יבוא CSV שהמשתמש מספק בעצמו.

## 8. בדיקות

בדיקה אוטומטית בסיסית:

```bash
node scripts/verify.mjs
```

Checklist ידני לפני פרסום:

- הזנת תאריך לועזי לפני ואחרי שקיעה נותנת תאריך עברי צפוי.
- כל חמש צורות הקלט של `תשמ״ט` מתקבלות.
- יום 21 מוצג `כ״א`, ושנת 5749 מוצגת `תשמ״ט`.
- אדר ב׳ חסום בשנה שאינה מעוברת ומופיע בשנה מעוברת.
- תאריך עתידי ותאריך עברי שאינו קיים מציגים שגיאה ברורה.
- הוספה, עריכה, מחיקה, רענון דף ומחיקת הכול פועלים.
- הרשימה המורחבת מציגה בדיוק 20 מועדים לכל אדם.
- קובץ ICS נפתח ביומן ונושא כותרת ותיאור ברורים.
- אין כפילויות לאחר סנכרון Google חוזר.
- עריכה או מחיקה ולאחריה סנכרון מעדכנות את היומן הייעודי.
- בדיקה במובייל, desktop, ניווט מקלדת, focus ותוכנת קוראת מסך בסיסית.
- דפי Privacy ו־Terms פתוחים מכל קישור footer.

## 9. פרסום

### OpenAI Sites

הפרויקט כבר מקושר באמצעות `.openai/hosting.json`. יש לשמור את אותו project ID, לבצע validation, לשמור version חדש ורק אז לפרוס אותו. הכתובת הפעילה היא כתובת production.

### GitHub Pages

ה־workflow ב־`.github/workflows/pages.yml` מעלה את `dist/` כאתר סטטי. סדר עבודה מומלץ:

1. צור repository חדש ב־GitHub.
2. העלה את כל תוכן החבילה, לא את קובץ ה־ZIP עצמו.
3. ב־Settings → Pages בחר Source: GitHub Actions.
4. דחוף ל־branch בשם `main` או הפעל את ה־workflow ידנית.
5. אם מפעילים Google, הוסף את כתובת Pages המדויקת ל־Authorized JavaScript origins.

## 10. סדר עדיפויות להמשך

1. יצירת Google OAuth Client ID והפעלת החיבור הישיר בסביבת test.
2. בדיקת קצה־לקצה בחשבון Google נפרד והשלמת פרטי Privacy/Terms.
3. פרסום גרסה עם Google פעיל.
4. הוספת export/import JSON או CSV לגיבוי הרשימה בין מכשירים.
5. רק אם יש צורך אמיתי: חשבון משתמש וסנכרון מאובטח בין מכשירים.
6. בחינת PWA/installable app ונגישות מתקדמת.

## 11. דברים שלא לעשות

- לא להשתמש באירוע Gregorian recurring שנתי במקום 20 תאריכים מחושבים.
- לא להעתיק תאריכים עבריים באמצעות טבלת קירוב או חישוב ידני חלקי.
- לא לשמור access token לאורך זמן בדפדפן.
- לא להכניס Client Secret ל־HTML, JavaScript, GitHub או Sites.
- לא ליצור Site חדש כאשר המטרה היא לעדכן את האתר הקיים.
- לא להסיר את רישיון ספריית `hdate`.
- לא לטעון נתוני Facebook או אנשי קשר בלי הרשאה, הסכמה ומדיניות תואמות.

## 12. מקורות

החלטות המחקר וקישורי המקור נמצאים ב־`RESEARCH.md`.
