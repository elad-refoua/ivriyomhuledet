# הוראות לקודקס בפרויקט עבריולדת

קרא את `CODEX_HANDOFF.md` במלואו לפני שינוי קוד.

## כללי עבודה

- זהו אתר סטטי בעברית ובכיוון RTL. קבצי הייצור עצמם נמצאים ב־`dist/`; אין כרגע שלב build.
- שמור על חוויית שימוש פשוטה, נגישה ורספונסיבית. אל תחליף את העיצוב הקיים בלי בקשה מפורשת.
- שמור על תצוגת תאריך עברי באותיות: יום ושנה עם גרש/גרשיים תקניים, למשל `כ״א באלול תשמ״ט`.
- קלט שנה חייב להמשיך לקבל לפחות: `תשמ״ט`, `התשמ״ט`, `תשמט`, `התשמט`, `5749`.
- לוח השנה הגרגוריאני אינו מתאים לאירוע חוזר שנתי בתאריך עברי. המשך ליצור אירועים מפורשים לכל שנה.
- כל אירוע חייב להסביר בכותרת ובתיאור שזה יום הולדת עברי, של מי הוא, מה התאריך העברי באותה שנה ומה היה תאריך הלידה העברי.
- אל תוסיף Google Client Secret או כל סוד אחר לצד הדפדפן. Client ID ציבורי בלבד מותר ב־HTML.
- אל תשמור access token ב־`localStorage`, ב־URL, בלוג או בקוד. המימוש הנוכחי שומר אותו בזיכרון בלבד.
- אל תשלח את רשימת האנשים לשרת בלי בקשה מפורשת, עיצוב פרטיות מתאים והסכמה ברורה של המשתמש.
- שמור את רישיון `dist/vendor/hdate/LICENSE` יחד עם ספריית הספק.
- לפני מסירה הרץ `node scripts/verify.mjs` ובדוק ידנית בדפדפן.

## פרסום

אם ממשיכים לפרסם ב־OpenAI Sites, קרא את `.openai/hosting.json` והשתמש באותו `project_id`. אל תיצור Site חלופי לאותו מוצר.

אם מפרסמים ב־GitHub Pages, פרסם את `dist/` באמצעות ה־workflow הקיים ועדכן את Authorized JavaScript origins ב־Google Cloud בהתאם לכתובת החדשה.
